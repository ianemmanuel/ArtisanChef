// Country/city shapes all come straight from the shared package
// (`@repo/types/admin-app`) — see `types/country.types.ts` for why this
// module is intentionally empty rather than deleted.
export {}
