// Country/city/vendor-type/document-type shapes all come straight from the
// shared package (`@repo/types/admin-app`) — it mirrors the backend's real
// return shapes, so there's nothing frontend-only to add here. Kept as an
// empty module (rather than deleted) because `types/index.ts` re-exports it.
export {}
